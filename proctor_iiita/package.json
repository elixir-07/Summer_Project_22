{
  "name": "proctor_iiita",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint 'src/**/*.js' --fix && prettier --write 'src/**/*.+(js|jsx|ts|tsx|json|scss|css)'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.20.0"
  }
}
